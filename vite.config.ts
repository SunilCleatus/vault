import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import { VitePWA } from 'vite-plugin-pwa';

export default defineConfig({
  plugins: [
    react(),
    VitePWA({
      registerType: 'autoUpdate',
      includeAssets: ['apple-touch-icon.png'],
      manifest: {
        name: 'Vault — Personal Document Vault',
        short_name: 'Vault',
        description: 'Your documents, organized in your own Google Drive.',
        theme_color: '#1f6f5c',
        background_color: '#0f1f1c',
        display: 'standalone',
        orientation: 'portrait',
        start_url: '/',
        icons: [
          { src: 'icons/icon-192.png', sizes: '192x192', type: 'image/png' },
          { src: 'icons/icon-512.png', sizes: '512x512', type: 'image/png' },
          {
            // Separate full-bleed source (no pre-rounded corners, artwork
            // shrunk to the safe zone) so Android's own adaptive-icon mask
            // doesn't clip into content baked into the regular icon.
            src: 'icons/icon-512-maskable.png',
            sizes: '512x512',
            type: 'image/png',
            purpose: 'maskable',
          },
        ],
      },
    }),
  ],
});
